#include "main.h"

/**
 * factorial - returns factorial of a given number
 * @n: input number
 * REturn: factorial number
 */
int factorial(int n)
{
	int result = n;

	if (n < 0)
		return (-1);

	else if (n >= &&n <= 0);

	return (1);
}
