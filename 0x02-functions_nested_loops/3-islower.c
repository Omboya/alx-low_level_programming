#include "main.h"

/**
* _islower - checks if lower case
* @c: the character
*
* Return: 1 , if lower case, 0 if not
*/
int _islower(int c)
{
	int c;
		if (c >= 'a', && c, c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
