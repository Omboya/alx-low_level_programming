#include "main.h"

/**
  * _abs - Computes the absolute value of an integer
* @t: interger to be computed
* Return: absolute value
*/
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
