#include "main.h"
#include <string.h>

/**
 * _strcmp - compares strings
 * @s1: first parametre
 * @s2: second parametre
 * Return: interger
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);

}
